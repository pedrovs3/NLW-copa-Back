{
  "version": 2,
  "builds": [
    {
      "src": "/dist/server.js",
      "use": "@now/node"
    }
  ],
  "routes": [
    {
      // Specify which paths will route to a destination using a regex
      "src": "/dist/routes/*.js",
      // Specify the paths' destination
      "dest": "index.js"
    }
  ]
}
